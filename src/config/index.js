const config = {
  APP_NAME: process.env.APP_NAME || "BOOK STORE",
  NAME_KEY: process.env.REACT_APP_NAME_KEY || "workforce",
  APP_PERSIST: process.env.REACT_APP_APP_PERSIST || "persist",
};
export default config;