import { baseRoutes } from "../../../helpers/baseRoutes";

const accesRoutes = {
    ADMINLOGIN: {
        path: `${baseRoutes.adminBaseRoute}/login`
    },
    ADMINDASH: {
        path: `${baseRoutes.adminBaseRoute}/dashboard`
    },
}

export default accesRoutes;